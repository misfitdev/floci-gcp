<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.floci</groupId>
    <artifactId>floci-gcp</artifactId>
    <version>0.8.0</version>
    <packaging>jar</packaging>

    <name>floci-gcp</name>
    <description>Free, MIT-licensed local GCP emulator — single-port, native-image, SDK-compatible</description>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <quarkus.platform.version>3.38.3</quarkus.platform.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.quarkus.platform</groupId>
                <artifactId>quarkus-bom</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Quarkus 3.38 manages httpclient5 at 5.6.x, whose new commons-compress content
                 decoder reaches the XZ, LZMA, Brotli and Zstd codecs. Under the native build's
                 link-at-build-time those need org.tukaani:xz, org.brotli:dec and zstd-jni on the
                 classpath, none of which this project ships, so the image build fails with
                 "unresolved method XZCompressorInputStream.builder()". docker-java is the only
                 consumer here and works with 5.5.x, which has no such decoder. -->
            <dependency>
                <groupId>org.apache.httpcomponents.client5</groupId>
                <artifactId>httpclient5</artifactId>
                <version>5.6.3</version>
            </dependency>
            <dependency>
                <groupId>com.google.cloud</groupId>
                <artifactId>libraries-bom</artifactId>
                <version>26.87.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Quarkus REST (RESTEasy Reactive) -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-jackson</artifactId>
          <!-- brotli4j resolves a JNI jar for the build host's platform (native-osx-aarch64 on a
               Mac, native-linux-* in CI); floci-gcp never enables Brotli HTTP compression. Maven
               wildcards match a whole artifactId only, hence the explicit list. -->
          <exclusions>
            <exclusion>
              <groupId>com.aayushatharva.brotli4j</groupId>
              <artifactId>native-osx-aarch64</artifactId>
            </exclusion>
            <exclusion>
              <groupId>com.aayushatharva.brotli4j</groupId>
              <artifactId>native-osx-x86_64</artifactId>
            </exclusion>
            <exclusion>
              <groupId>com.aayushatharva.brotli4j</groupId>
              <artifactId>native-linux-aarch64</artifactId>
            </exclusion>
            <exclusion>
              <groupId>com.aayushatharva.brotli4j</groupId>
              <artifactId>native-linux-x86_64</artifactId>
            </exclusion>
            <exclusion>
              <groupId>com.aayushatharva.brotli4j</groupId>
              <artifactId>native-windows-x86_64</artifactId>
            </exclusion>
          </exclusions>
        </dependency>

        <!-- YAML config support -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-config-yaml</artifactId>
        </dependency>

        <!-- Quarkus gRPC (provides @GrpcService, HTTP/2 via ALPN on shared port) -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-grpc</artifactId>
        </dependency>

        <!-- Vert.x for async processing -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-vertx</artifactId>
        </dependency>

        <!-- Google common protos (google.rpc.Status, google.api.*, etc.) -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>proto-google-common-protos</artifactId>
            <version>2.75.0</version>
        </dependency>

        <!-- Protobuf JSON support for REST services backed by Google proto contracts -->
        <dependency>
            <groupId>com.google.protobuf</groupId>
            <artifactId>protobuf-java-util</artifactId>
        </dependency>

        <!-- Cloud Run v2 proto types for REST JSON control-plane emulation -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>proto-google-cloud-run-v2</artifactId>
        </dependency>

        <!-- Cloud Functions v2 proto types for REST JSON control-plane emulation -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>proto-google-cloud-functions-v2</artifactId>
        </dependency>

        <!-- Eventarc v1 proto types for REST JSON control-plane emulation -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>proto-google-cloud-eventarc-v1</artifactId>
        </dependency>

        <!-- Service Usage v1 proto types for REST JSON control-plane emulation -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>proto-google-cloud-service-usage-v1</artifactId>
        </dependency>

        <!-- Pub/Sub gRPC stubs (PublisherGrpc, SubscriberGrpc ImplBase) -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>grpc-google-cloud-pubsub-v1</artifactId>
            <version>1.154.0</version>
        </dependency>

        <!-- google.iam.v1.IAMPolicy gRPC stub (IAMPolicyGrpc ImplBase) -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>grpc-google-iam-v1</artifactId>
        </dependency>

        <!-- Secret Manager gRPC stubs (SecretManagerServiceGrpc ImplBase) -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>grpc-google-cloud-secretmanager-v1</artifactId>
        </dependency>

        <!-- Cloud Logging gRPC stubs (LoggingServiceV2Grpc ImplBase) -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>grpc-google-cloud-logging-v2</artifactId>
        </dependency>

        <!-- Cloud Monitoring gRPC stubs (MetricServiceGrpc ImplBase) -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>grpc-google-cloud-monitoring-v3</artifactId>
        </dependency>

        <!-- Cloud KMS gRPC stubs (KeyManagementServiceGrpc ImplBase) -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>grpc-google-cloud-kms-v1</artifactId>
        </dependency>

        <!-- Cloud Storage v2 gRPC stubs (StorageGrpc ImplBase) -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>grpc-google-cloud-storage-v2</artifactId>
        </dependency>

        <!-- Cloud Tasks gRPC stubs (CloudTasksGrpc ImplBase) -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>grpc-google-cloud-tasks-v2</artifactId>
        </dependency>

        <!-- Cloud Scheduler gRPC stubs (CloudSchedulerGrpc ImplBase) -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>grpc-google-cloud-scheduler-v1</artifactId>
        </dependency>

        <!-- Firestore gRPC stubs (FirestoreGrpc ImplBase) -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>grpc-google-cloud-firestore-v1</artifactId>
        </dependency>

        <!-- Datastore proto types (LookupRequest, CommitRequest, etc.) -->
        <dependency>
            <groupId>com.google.api.grpc</groupId>
            <artifactId>proto-google-cloud-datastore-v1</artifactId>
        </dependency>

        <!-- cron-utils for Cloud Scheduler unix-cron schedule parsing -->
        <dependency>
            <groupId>com.cronutils</groupId>
            <artifactId>cron-utils</artifactId>
            <version>9.2.1</version>
        </dependency>

        <!-- Jackson -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>
        <!-- CBOR used internally by WalStorage for binary WAL entries -->
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-cbor</artifactId>
        </dependency>

        <!-- BouncyCastle — X.509 generation for self-signed TLS certificates -->
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk18on</artifactId>
            <version>1.85.2</version>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcpkix-jdk18on</artifactId>
            <version>1.85</version>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit-mockito</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Docker client -->
        <dependency>
            <groupId>com.github.docker-java</groupId>
            <artifactId>docker-java-core</artifactId>
            <version>3.7.1</version>
        </dependency>
        <dependency>
            <groupId>com.github.docker-java</groupId>
            <artifactId>docker-java-transport-httpclient5</artifactId>
            <version>3.7.1</version>
        </dependency>
        <!-- Explicit overrides required because quarkus-bom pins these at 3.7.0 -->
        <dependency>
            <groupId>com.github.docker-java</groupId>
            <artifactId>docker-java-api</artifactId>
            <version>3.7.1</version>
        </dependency>
        <dependency>
            <groupId>com.github.docker-java</groupId>
            <artifactId>docker-java-transport</artifactId>
            <version>3.7.1</version>
        </dependency>
        <!-- Brotli required by docker-java-transport-httpclient5:3.7.1 for native image -->
        <dependency>
            <groupId>org.brotli</groupId>
            <artifactId>dec</artifactId>
            <version>0.1.2</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>io.quarkus.platform</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.6</version>
                <configuration>
                    <systemPropertyVariables>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.15</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals><goal>prepare-agent</goal></goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals><goal>report</goal></goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.6.3</version>
                <executions>
                    <execution>
                        <id>enforce-java</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[25,26)</version>
                                    <message>Floci requires JDK 25. Set JAVA_HOME to JDK 25.</message>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>native</id>
            <activation>
                <property>
                    <name>native</name>
                </property>
            </activation>
            <properties>
                <skipITs>true</skipITs>
                <quarkus.native.enabled>true</quarkus.native.enabled>
                <maven.compiler.release>25</maven.compiler.release>
            </properties>
        </profile>
    </profiles>
</project>
